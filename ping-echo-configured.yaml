name: Ping Echo Configured
entryKey: ping-echo-configured
shortDescription: Basic ping/echo server with user configuration
description: |
  A single-user example server that prompts the connecting user for a GREETING
  value and uses it as an echo response prefix. The echo response also includes
  the catalog-managed sensitive SECRET_KEY value for verification.
runtime: containerized
containerizedConfig:
  image: ghcr.io/calvinmclean/mcp-ping-echo:main
  port: 8080
  path: /mcp
  startupTimeoutSeconds: 30
config:
  - name: Greeting
    usage: env
    key: GREETING
    description: Prefix added to every echo response, for example "Hello".
    required: true
  - name: Static Secret
    usage: env
    key: SECRET_KEY
    description: Example catalog-managed sensitive environment variable.
    value: example-secret-value
    sensitive: true
toolPreview:
  - name: ping
    description: Return pong from the server.
    params: {}
  - name: echo
    description: Echo a message back to the caller with the configured greeting.
    params:
      message: Message to echo
